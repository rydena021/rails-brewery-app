<%= simple_form_for @brewery do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :description %>
  <%= f.input :photo, as: :attachinary %>
  <%= f.submit "Add Brewery", {class: "btn btn-primary"} %>
<% end %>
